#include <stdio.h>
#include <unistd.h> 
#include <stdbool.h>
#include <math.h>

void checkBalance(float balance);
float withdraw(float balance);
float deposit();

int main(float balance){
    return 0;
}

void checkBalance(float balance){
    float balance = 0.00f;
    subtract(deposit - w)
    
}