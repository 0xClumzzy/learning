Numbers that appear that appear to be random numnbers but are determined by a mathematical formula that uses a seed value to generate a predictable sequence of numbers
needs:
`<stdlib.h>`
`<time.h>`

Generating the seed value
```C
srand(time(NULL));
```

